/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // autorização p/ exibir variável de ambiente no navegador
  // env: {
  //   MYSQL_CONNECT: process.env.MYSQL_CONNECT,
  // },
};

export default nextConfig;
